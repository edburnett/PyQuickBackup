Add logging.
Create object-oriented version/branch.
Add more error-checking
check to see if it's running as root/sudo, if not, notify and exit
add command-line options (?), and options to enable/disble copying
add optional GUI (?)
add GUI browsing and restoring from archives
