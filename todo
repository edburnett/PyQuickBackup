Add logging. create log and then add it to the archive just before moving/deleting it?
Create object-oriented version/branch.
Add more error-checking
cehck to see which python version is running and adjust code accordingly (platform.version())
check to see if it's running as root/sudo, if not, notify and exit
add check using platforum module to find out which distro is being used, and export list of explicitly-installed programs from that distro's package manager, test on popular distros (arch, mint, ubuntu, gentoo, debian, centos)
add command-line options (?), and options to enable/disble copying
add optional GUI (?)
add GUI browsing and restoring from archives
